# deploy-trigger: 2026-05-30T01:33:00Z — v30.10 fix E8/E9 echo type validation
services:
  - type: web_service
    name: trinity-agent-issue-gateway
    runtime: node
    region: oregon
    plan: free
    rootDir: examples/github-app-backend
    buildCommand: pip install -r ../../requirements-ci.txt && yarn
    startCommand: yarn start
    healthCheckPath: /health
    autoDeploy: true
    branch: main
    envVars:
      - key: PYTHON_VERSION
        value: "3.12"
