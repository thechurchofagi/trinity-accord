# deploy-trigger: 2026-05-29T09:32:17Z — v30.7.2 receipt triage fix
services:
  - type: web_service
    name: trinity-agent-issue-gateway
    runtime: node
    region: oregon
    plan: free
    rootDir: examples/github-app-backend
    buildCommand: pip install -r ../../requirements-ci.txt && yarn
    startCommand: yarn start
    healthCheckPath: /health
    autoDeploy: true
    branch: main
    envVars:
      - key: PYTHON_VERSION
        value: "3.12"
