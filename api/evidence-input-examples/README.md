# Evidence Input Examples

These files are examples, not real verification evidence.

- `templates/` contain placeholders and must fail Claim Gate unchanged.
- `tutorial/` files teach structure and are intentionally non-passing.
- `test-fixtures/` files are synthetic data for repository tests only.

Do not submit any example unchanged as real verification evidence.

If you copy example values into a real verification claim, Claim Gate must fail.
