name = "echo-submission-proxy"
main = "src/index.js"
compatibility_date = "2024-12-01"
workers_dev = true

# Deprecated tombstone worker only.
# No submission, GitHub token, Turnstile, KV, or cron is required.
# /submit-echo returns HTTP 410 Gone.
