name: Test Dispatch

on:
  workflow_dispatch:
    inputs:
      test_input:
        description: 'Test input'
        required: false
        type: string

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Test dispatch triggered with input: ${{ github.event.inputs.test_input }}"
