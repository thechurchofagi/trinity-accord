Full Project Preservation + Harvard publication activation v1.

Capture the safe current repository baseline, every custom GitHub Release asset,
and exact published Polygon/Base sidechain DOI bytes into one content-addressed,
non-amending preservation bundle. No Arweave write is authorized by this trigger.

After the full bundle succeeds, Harvard Dataverse publication is authorized only
through the dedicated fail-closed publisher: preserve the exact GitHub artifact
bytes as an opaque file, publish as a non-amending archival mirror, and require a
complete public-byte SHA-256 readback before the final receipt is released.

Harvard Dataverse publication authorized: v1
Authority role: non-amending archival mirror; canonical authority unchanged.
Activation source: post-entry-fix verified workflow run; actionlint clean on both preservation workflows.
